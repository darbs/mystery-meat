module.exports = function (state) {

    var TreeModel = require('tree-model'),
        // potential custom modelComparatorFn
        tree = new TreeModel();

    var dis = ['north', 'south', 'east', 'west'];
};